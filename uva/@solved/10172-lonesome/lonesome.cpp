#include <iostream>
#include <vector>
using namespace std;

int main() {
    int s;
    cin >> s;
    while (s--) {
        int n, s, q;
        cin >> n >> s >> q;
        vector<vector<int>> v;
    }
}