#include <bits/stdc++.h>
#define INF 1e9
#define LLINF 4e18
using namespace std;

typedef long long int ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<vi> vvi;
typedef vector<ii> vii;

// prev_state: is bottom drawer locked?
// curr_drawer: which drawer i am on rn
// num_locked: number of items i have to lock
// dp[prev_state][curr_drawer][num_locked]
//      if ()

int main() {
    
    return 0;
}