#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<ii> vii;

const int INF = 1e9;
const ll LLINF = 4e18;
const double EPS = 1e-9;

int N, k;
int grid[80][80];

int memo[]

int dp(int i, int j, int k_left, int )

int main() {
    while (cin >> N >> k && !(N == 0 && k == 0)) {
        for (int i = 0; i < N; i++) {
            for (int j = 0; j < N; j++) {
                cin >> grid[i][j];
            }
        }
    }
    return 0;
}